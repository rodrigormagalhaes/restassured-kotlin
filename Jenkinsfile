pipeline {
    agent { docker 'openjdk13' }
    stages {
        stage('Test Rodrigo') {
            steps {
                echo 'Hello, JDK'
                sh 'java -version'
            }
        }
    }
}